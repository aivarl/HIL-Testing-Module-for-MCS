/**
 * System components configuration.
 */
// Add constant to module.
angular
    .module('MCS')
    .constant("SYSCOMPONENTS", {
        SYSTEM_COMPONENTS: {
            STATUS_CHECK_INTERVAL: 3000,
        }
    })